# 작업 로그 — 2026-05-30 Phase 2~4

## 완료한 작업

### Phase 2 (파일 탐색기 + 배포)
- `components/FileExplorer.tsx` — 재귀 파일 트리, 클릭으로 디렉토리 펼치기
- `components/CodeEditor.tsx` — Monaco Editor, Ctrl+S 저장, dirty 표시
- `components/DeployPanel.tsx` — SSE 실시간 배포 로그, 터미널 스타일
- `components/ProjectDetail.tsx` — 3-패널 레이아웃 (트리 | 에디터 | AI/정보)
- `app/projects/[id]/page.tsx` — 프로젝트 상세 페이지

### Phase 3 (AI 에디터)
- `components/AIEditor.tsx` — 프로바이더 선택, 프롬프트 입력, diff 미리보기, 적용
- DB에서 등록된 프로바이더 자동 로드
- 적용 시 파일 저장 + 코드 에디터 내용 업데이트

### Phase 4 (시각적 편집기)
- `app/api/proxy/route.ts` — 내부 서비스 프록시 + 클릭 스크립트 주입
- `components/VisualEditor.tsx` — iframe + postMessage + 소스 파일 검색 + AI 수정
- `app/editor/[projectId]/page.tsx`

### 기타
- `docker-compose.yml` — 미니PC 배포용
- `npm run build` 성공 (15개 라우트)

## 결정사항

- AI 에디터는 ProjectDetail의 우측 탭으로 통합 (별도 페이지 불필요)
- 시각적 편집기의 iframe은 `sandbox="allow-scripts allow-same-origin"` — 클릭 스크립트 실행에 필요
- postMessage로 클릭 이벤트 전달 (same-origin 정책 우회)

## 다음 에이전트가 할 것 (없음)

모든 Phase 완료. 남은 것은 미니PC 배포 (사용자 직접 수행).
